# Agent handoff reports

Session reports are generated by `scripts/ci/agent-session.mjs logout` at `diagnostics/agents/handoffs/<sessionId>.json`.

A successor agent must ingest the predecessor report with `--from-session=<previous-session>` before continuing inherited work.
