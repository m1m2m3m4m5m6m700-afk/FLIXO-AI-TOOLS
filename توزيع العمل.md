# FLIXO-AI-TOOLS — توزيع العمل

> هذا الملف مخصص **لتوزيع العمل بين الوكلاء فقط**.
> لا يمثل مصدر المهام الرئيسي، ولا ينشئ سلطة جديدة، ولا يسمح للوكلاء بالنشر المباشر.
> 
> القاعدة التشغيلية: `Agent isolated workspace → Result/Patch → Chair 1 aggregation → Verification → Publication`.

-

## ACTIVE REPAIR WAVE — 3 ISOLATED AGENTS — 2026-09-22

## EXCLUSIVE TASK CLAIM PROTOCOL — V1

**قاعدة أساسية:** المهمة الموجودة تحت `AVAILABLE TASKS` متاحة لوكيل واحد فقط.  
عندما يلتزم وكيل بالمهمة، يجب في **نفس عملية التسجيل** حذفها من `AVAILABLE TASKS` ونقلها إلى `CLAIMED / IN PROGRESS` مع تسجيل هوية الوكيل و`taskId` و`entrySha`.

**منع التكرار:**
1. الوكيل لا يبدأ التنفيذ قبل تسجيل claim.
2. إذا لم تعد المهمة موجودة تحت `AVAILABLE TASKS` فهي **محجوزة** ولا يجوز لوكيل آخر تنفيذها.
3. الوكيل الثاني الذي يجد المهمة محجوزة يجب أن يرفضها: `TASK_ALREADY_CLAIMED`.
4. لا يجوز نسخ نفس `taskId` إلى وكيل آخر.
5. إعادة المهمة إلى `AVAILABLE TASKS` لا تتم إلا بواسطة Chair 1 بعد إغلاق/إلغاء claim بشكل موثق.
6. التغيير في هذا الملف نفسه يخضع لسلطة النشر المعتمدة؛ العامل لا ينشئ فرعًا ولا يدفع إلى `execution`.

### AVAILABLE TASKS

> المهام هنا فقط هي المهام التي لم يحجزها أي وكيل بعد.

> `REPAIR-WAVE-3-AGENT-01-SHA-GOVERNANCE` حُجزت الآن حصريًا بواسطة `MASTER-3 / GPT-5.6 LUNA`، ولذلك لا تظهر ضمن المتاح.

### CLAIMED / IN PROGRESS

| Task ID | Agent | State | Entry SHA | Claim Evidence |
|---|---|---|---|---|
| REPAIR-WAVE-3-AGENT-01-SHA-GOVERNANCE | MASTER-3 / GPT-5.6 LUNA | CLAIMED / IN PROGRESS | 05dfe89243aed233a58d8dd9b253e3340b01e4f4 | Claim recorded on execution head before mutation; exclusive ownership asserted. |

### CLOSED / HANDED TO CHAIR 1

| Task ID | Agent | State | Result |
|---|---|---|---|
| — | — | — | — |


### TASK CLAIM RULE FOR THE CURRENT WAVE

عند إسناد أي من المهام الثلاث الحالية:
- يُحذف `TASK ID` من `AVAILABLE TASKS` فورًا.
- يُضاف سجل وحيد له في `CLAIMED / IN PROGRESS`.
- لا يمكن لوكيلين امتلاك نفس `TASK ID`.
- بعد انتهاء العمل، لا تعاد المهمة إلى قائمة الإتاحة تلقائيًا؛ تنتقل إلى `CLOSED / HANDED TO CHAIR 1` مع نتيجة قابلة للتعديل من Chair 1.

### WAVE BASELINE

- Prior observed execution before dispatch: `7a678e8f5bca0378b57eefb77b3519f1f0843799`
- Canonical main observed: `5f340c4338abbfca299223309544946e97d9ccb2`
- Dispatch commit: this section's commit is itself a repository mutation; therefore the final worker `entrySha` MUST be captured by Chair 1 immediately after dispatch and recorded in each worker result.
- Topology: `execution → main ONLY`
- Agent mode: `FLIXO-AGENT-ISOLATED-WORKSPACE-v1`
- Worker publication: `FORBIDDEN`
- Final aggregation/edit/commit/push authority: `CHAIR_1`
- Rule: every agent starts from the baseline snapshot only and never follows a moving branch head after task start.

### LAST OBSERVED RED — ROOT-CAUSE TARGETS

#### RED-001 — Exact-SHA promotion proof supersession
Observed in run `35706447705` / job `106676429367`.

Evidence:
```
EXPECTED_SHA=8218f95aeeca1412633edd24ad24360aebec0f25
FAIL CLOSED: execution SHA 7a678e8f5bca0378b57eefb77b3519f1f0843799 != PR head 8218f95aeeca1412633edd24ad24360aebec0f25
FAIL CLOSED: commit 8218f95aeeca1412633edd24ad24360aebec0f25 is superseded by 7a678e8f5bca0378b57eefb77b3519f1f0843799 on .../execution.
```

#### RED-002 — WP0 exact-SHA checkout supersession
Observed in run `35706447596` / job `106676428152`.

The job checked out `8218f95...` while `execution` had already advanced to `7a678e8...`, and failed closed in `assert-current-commit.mjs`.

#### RED-003 — Test Impact exact-SHA supersession
Observed in run `35706447647` / job `106676428374`.

The Test Impact job likewise operated from the older `8218f95...` snapshot. Treat this as the same SHA-race family until current-head verification proves a different root cause.

#### TOPOLOGY-001 — Non-canonical branch refs observed
Observed refs:
- `chair1/atomic-fix-execution-gate-test-drift-20260922`
- `chair1/atomic-test-execution-gate-drift-20260922-r2`

These violate the intended two-branch active topology. Agents MUST NOT create additional branches. Cleanup/removal of existing extra refs is a Chair-1-controlled administrative action.

### AGENT-1 — SHA / CI GOVERNANCE REPAIR

**Task ID:** `REPAIR-WAVE-3-AGENT-01-SHA-GOVERNANCE`

**Objective:** eliminate the SHA supersession race so every verification run proves the exact source SHA it was dispatched for, without falsely binding to a moving `execution` head.

**Primary targets:**
- `scripts/ci/assert-current-commit.mjs`
- `FLIXO Latest Commit Test Supersession`
- `FLIXO Auto Repair Merge Gate`
- `FLIXO WP0 Trust Baseline`
- `FLIXO Test Impact`
- `FLIXO Test Impact Execution`
- their exact-SHA checkout/assertion handoff only

**Acceptance:**
- run source SHA, checkout SHA, verification SHA, PR head SHA and observed execution provenance are explicit and internally consistent;
- superseded historical runs remain correctly non-authoritative;
- a newer execution head does not make a valid newer run fail because an older run is still executing;
- no `|| true`, test weakening, or stale-evidence acceptance;
- no mutation outside assigned governance scope;
- produce `READY_FOR_CHAIR1` patch/evidence only.

**Do NOT edit:** application source, unrelated test implementations, security policy, main, or create any branch.

### AGENT-2 — TEST / APPLICATION REDUCTION

**Task ID:** `REPAIR-WAVE-3-AGENT-02-TESTS-APPLICATION`

**Objective:** eliminate genuine product/test failures that remain after the SHA race is separated from test execution.

**Primary targets:**
- `src/`
- `tests/`
- test/build/static/browser contracts and their direct implementation code
- only the concrete application/test files implicated by fresh exact-SHA evidence

**Acceptance:**
- reproduce the failure from the worker's entry snapshot;
- identify direct root cause, not symptom;
- make the smallest causal source/test-compatible repair;
- run targeted regression plus relevant build/static tests;
- never weaken assertions, skip tests, change `only`/skip semantics, or modify CI gates;
- return a patch that Chair 1 can rebase onto the latest execution state.

**Do NOT edit:** CI governance, Chair protocols, repair-control-plane, security workflows, or any branch.

### AGENT-3 — SECURITY / REPAIR AUTOMATION

**Task ID:** `REPAIR-WAVE-3-AGENT-03-SECURITY-AUTOMATION`

**Objective:** eliminate genuine failures in repository security, advanced repair contracts, and automated repair merge controls.

**Primary targets:**
- `.github/workflows/repository-security-baseline.yml`
- `.github/workflows/claude-security-review.yml`
- `.github/workflows/advanced-repair-contract.yml`
- `.github/workflows/auto-repair-merge-gate.yml`
- directly implicated `scripts/ci/auto-repair/*` files only

**Acceptance:**
- distinguish provider/external failures from repository defects;
- prove every internal defect with fresh exact-SHA evidence;
- preserve fail-closed behavior;
- preserve Chair-1 publication authority and isolated-worker protocol;
- repair root causes without weakening security or merge controls;
- return only a candidate patch/evidence packet to Chair 1.

**Do NOT edit:** application source or Agent-1 SHA gate files.

### SHARED RULES FOR ALL 3

1. Chair 1 first snapshots the post-dispatch `execution` SHA. Every worker starts from that exact immutable `entrySha`.
2. Worker never reads another worker's local workspace.
3. Worker never commits or pushes.
4. Worker never changes `execution` directly.
5. Worker result is editable by Chair 1.
6. Any cross-scope conflict is reported as `CHAIR1_INTEGRATION_REQUIRED`; the worker stops at the boundary rather than modifying another agent's scope.
7. A targeted PASS is not project GREEN.
8. Historical PASS is not current PASS.
9. Any current SHA movement is handled by Chair 1 during aggregation.
10. After Chair 1 integrates the three result packets, the complete required CI surface is rerun on the resulting exact SHA.
11. Continue the repair wave until there are zero unprocessed actionable RED checks, or a separately proven external blocker is recorded.

### CHAIR-1 INTEGRATION ORDER

`COLLECT AGENT-1 + AGENT-2 + AGENT-3 → SNAPSHOT CURRENT EXECUTION/MAIN → RECONCILE → EDIT/REMOVE/UPGRADE AS NEEDED → TARGETED REGRESSION → FULL EXACT-SHA CI → REPEAT`

