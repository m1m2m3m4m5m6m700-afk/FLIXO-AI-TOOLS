# FLIXO-AI-TOOLS — بوابة المهام التنفيذية

> **الحالة:** ACTIVE / REQUIRED AGENT GATE
> **الغرض:** هذه الوثيقة هي قائمة المهام المفتوحة التنفيذية للمشروع. يجب على كل وكيل قراءتها قبل أي تنفيذ حتى يعرف ما هو المطلوب، وما هو المسموح له، وما الذي لا يجوز إعادة فتحه.
> **نطاقها:** مهام `main` الحالية فقط. لا تعتمد على ذاكرة المحادثة، أو فروع قديمة، أو تقارير تاريخية باعتبارها حالة حالية.

## 0. قاعدة الدخول الإلزامية

قبل أي قراءة للكود أو تعديل أو اختبار أو تشغيل CI، يجب على الوكيل تنفيذ التسلسل التالي:

```text
PROJECTS.md
→ المهام.md
→ AGENTS.md
→ INGEST HANDOFF
→ RESOLVE EXACT main SHA
→ IDENTIFY ACTIVE TASK
→ READ AUTHORITATIVE CONTRACT
→ RCA / SCOPE LOCK
→ EXECUTE
→ TARGETED REGRESSION
→ AFFECTED CONTRACT VERIFICATION
→ EXACT-SHA PROOF
→ UPDATE PROJECTS.md
→ HANDOFF
```

`المهام.md` ليست اقتراحًا ولا قائمة أفكار. هي بوابة نطاق العمل. عدم قراءتها يعني أن الوكيل لا يملك نطاقًا تنفيذيًا معتمدًا.

## 1. مبادئ الالتزام الصارمة

1. `main` هو مصدر الحقيقة الحالي.
2. لا يوجد تنفيذ لمهمة غير موجودة هنا إلا إذا تم أولًا إدراجها كمهمة معتمدة ومحددة المصدر.
3. `ACTIVE` فقط هو التنفيذ المصرح به مباشرة.
4. `LOCKED` لا يعني منسية؛ يعني أن dependency gate لم يكتمل.
5. `CANDIDATE` ليست تفويضًا بالتنفيذ.
6. لا يُعاد فتح مهمة مغلقة لأن اسمها يبدو مفيدًا؛ يجب إثبات وجود RCA أو invariant جديد.
7. لا يوجد GREEN من اختبار جزئي، أو فرع غير مدمج، أو دليل قديم، أو SHA مختلف، أو provider bypass.
8. لا اختبارات يتم تعطيلها، أو assertions يتم تخفيفها، أو failures يتم إخفاؤها، أو coverage يتم حذفها من أجل Green.
9. لا LLM-direct execution، ولا تجاوز authorization، أو policy، أو approval، أو evidence.
10. لا secrets في browser bundle، ولا إنشاء store أو registry أو validator مكرر دون حاجة مثبتة.
11. كل إصلاح يبدأ بـ RCA واضح وينتهي بدليل Exact-SHA.
12. عند تحرك `main` يجب إعادة حل الـSHA قبل مواصلة التنفيذ.

## 2. قاعدة الملكية

لكل مهمة:

- مالك تنفيذ واحد في كل لحظة.
- RCA واحد واضح لكل defect.
- Contract/plan authoritative محدد.
- Exact base/head SHA عند بدء أو إنهاء التنفيذ.
- Evidence مرجعي عند توفره.
- Next deterministic action محدد.

## 3. قائمة المهام المفتوحة

| ID | المهمة | الحالة | المرجع الحاكم | العمل التالي المحدد |
|---|---|---|---|---|
| ADMIN-003 | Admin Truth / Command / Security / Contract / Operations / Incident Centers | **ACTIVE** | `docs/ADMIN-CONTROL-PLANE-MASTER-PLAN.md` | تشغيل canonical CI على `main` الحالي، ثم إغلاق الإصلاحات فقط بدليل Exact-SHA |
| ADMIN-004 | Admin Controlled Execution + Rollback | **LOCKED** | `docs/ADMIN-CONTROL-PLANE-MASTER-PLAN.md` | تفعيل بعد إثبات authorization + policy + evidence + audit + rollback في ADMIN-003 |
| ADMIN-005 | Admin Change / Approval / Incident Consolidation | **LOCKED** | `docs/ADMIN-CONTROL-PLANE-MASTER-PLAN.md` | يبدأ بعد ADMIN-004 |
| ADMIN-006 | Admin Truth Graph | **LOCKED** | `docs/ADMIN-CONTROL-PLANE-MASTER-PLAN.md` | يبدأ بعد ثبات provenance graph الحقيقي |
| ADMIN-007 | Controlled AI Assistant | **LOCKED** | `docs/ADMIN-CONTROL-PLANE-MASTER-PLAN.md` | يبدأ بعد ثبات authorization + deterministic execution + evidence |
| ADMIN-008 | Admin Production Certification | **LOCKED** | `docs/ADMIN-CONTROL-PLANE-MASTER-PLAN.md` | شهادة نهائية على candidate SHA واحد، متضمنة live deployed-SHA evidence |
| BUILD-002 | Residual lazy-chunk → entry dependency candidates | **CANDIDATE** | `PROJECTS.md` + build-boundary contract | تحليل fresh artifact graph أولًا؛ لا تعديل قبل إثبات defect حالي |
| I18N-001 | MutationObserver ownership reanalysis | **CANDIDATE** | `PROJECTS.md` + i18n contracts | تتبع ownership/lifecycle من runtime قبل أي repair |
| TEST-001 | Playwright surface ownership cleanup | **CANDIDATE** | `PROJECTS.md` + Playwright surface contract | حصر ownership ثم إزالة unauthorized surfaces فقط |
| DEBT-001 | Technical-debt audit candidates | **CANDIDATE** | `PROJECTS.md` + current evidence | مراجعة fresh-failure / zero-value candidates فقط |

## 4. مهام ممنوع إعادة إدراجها في القائمة المفتوحة

هذه المهام مغلقة حاليًا ولا تُعاد إلى Active Queue بسبب الذاكرة أو الرغبة في التوسع:

```text
ARCH-001  = CLOSED / VERIFIED
AGENT-001 = CLOSED / VERIFIED
ADMIN-001 = CLOSED / VERIFIED
ADMIN-002 = CLOSED / VERIFIED
```

إعادة فتح أي منها تحتاج RCA جديدًا مثبتًا أو متطلبًا جديدًا صريحًا، مع تحديث `PROJECTS.md` قبل التنفيذ.

## 5. قاعدة ترتيب Admin

```text
ADMIN-002  CLOSED
      ↓
ADMIN-003  ACTIVE
      ↓
ADMIN-004  LOCKED
      ↓
ADMIN-005  LOCKED
      ↓
ADMIN-006  LOCKED
      ↓
ADMIN-007  LOCKED
      ↓
ADMIN-008  FINAL CERTIFICATION
```

فشل Vercel الخارجي لا يوقف المهام المستقلة في `ADMIN-003..007`. يبقى live deployment proof مسارًا خاصًا بـ`ADMIN-008`.

## 6. بروتوكول تنفيذ المهمة

لكل مهمة، يجب تسجيل:

```text
ENTRY SHA
TASK ID
RCA / INVARIANT
AUTHORITATIVE CONTRACT
CHANGED FILES
TARGETED REGRESSION
AFFECTED CONTRACT GRAPH
CI / CERTIFICATION RUN
EXACT-SHA EVIDENCE
EXIT SHA
REMAINING WORK
```

لا يجوز اعتبار المهمة منجزة لأن الكود موجود فقط. الإنجاز = سلوك مثبت على SHA محدد.

## 7. بروتوكول الحذف بعد التنفيذ

الهدف من الحذف هو تنظيف **قائمة المهام المفتوحة**، وليس حذف الذاكرة الهندسية.

لا يجوز حذف أي مهمة من هذا الملف إلا بعد تحقق جميع الشروط التالية:

```text
MERGED TO MAIN
∧
EXACT MAIN SHA CONFIRMED
∧
REQUIRED TARGETED TESTS PASS
∧
REQUIRED CI / CERTIFICATION PASS
∧
TASK INVARIANT PROVEN
∧
NO DEPENDENT REPAIR OPEN
∧
CLOSURE EVIDENCE RECORDED
∧
PROJECTS.md CLOSED HISTORY UPDATED
∧
NEXT TASK / DEPENDENCY STATE UPDATED
```

حذف المهمة قبل اكتمال أي شرط = **PROTOCOL VIOLATION**.

عند اكتمال الشروط:

1. تُزال المهمة من **Open Task Queue** في `المهام.md`.
2. يُسجل `taskId + closure SHA + CI run + evidence + verdict` في `PROJECTS.md` → `Closed History`.
3. لا تُحذف RCA أو invariant أو evidence أو closure SHA من سجل المشروع.
4. لا يُعاد استخدام دليل المهمة المغلقة لإثبات SHA لاحق.
5. إذا ظهرت مشكلة جديدة بعد الحذف، تُنشأ مهمة جديدة بمعرف جديد أو RCA جديد بدل إعادة تدوير الإغلاق القديم.

## 8. حالات المهام

```text
ACTIVE
LOCKED
CANDIDATE
NEEDS DEVELOPMENT
DEFER
BLOCKED
IMPLEMENTED / VERIFICATION PENDING
CLOSED / STABLE
CANCELLED
```

قواعد التفسير:

```text
CANDIDATE ≠ ACTIVE
DEFER ≠ forgotten
BLOCKED ≠ STOP
UNKNOWN ≠ PASS
INCOMPLETE ≠ SUCCESS
```

## 9. قاعدة منع اختلاق المهام

لا يضيف الوكيل مهمة لأن:

- هناك فكرة مستقبلية فقط.
- feature تبدو مفيدة.
- تقريرًا تاريخيًا اقترح شيئًا لم يعد قائمًا.
- اسم ملف قديم يوحي بوجود gap.
- provider فشل.
- اختبارًا فرعيًا فشل دون RCA.

أي مهمة جديدة يجب أن تحتوي على:

```text
stable ID
purpose
source / contract
current evidence
owner
scope
exit criteria
next deterministic action
```

## 10. واجب الوكيل عند عدم وجود ACTIVE صالح

إذا لم توجد مهمة `ACTIVE` قابلة للتنفيذ:

```text
DO NOT INVENT WORK
DO NOT START CANDIDATE WORK
DO NOT UNLOCK DEPENDENCIES SPECULATIVELY
```

بل يجب تحديث الحالة من الدليل الفعلي واختيار المهمة التالية التي أصبحت claimable وفق dependencies الموثقة.

## 11. العلاقة مع PROJECTS.md وAGENTS.md

`PROJECTS.md` يحفظ خريطة المشاريع، حالة الاعتماديات، وسجل الإغلاق.

`المهام.md` يحفظ قائمة العمل المفتوحة وتعليمات حذفها وإعادة فتحها.

`AGENTS.md` يحكم بروتوكول دخول الوكيل والتنفيذ والتنسيق والجلسة.

لا يجوز أن ينسخ أحد هذه الملفات ملكية الآخر. عند التعارض، يجب إيقاف الادعاء بالـGREEN وتحديد مصدر الحقيقة الحاكم لكل معلومة.

## 12. الحالة المرجعية عند إنشاء هذه البوابة

```text
MAIN SHA = ed2402fac16956d6a6954740ac1ed6d8a8d59b87
ACTIVE   = ADMIN-003
LOCKED   = ADMIN-004..ADMIN-008
CANDIDATE = BUILD-002, I18N-001, TEST-001, DEBT-001
CLOSED   = ARCH-001, AGENT-001, ADMIN-001, ADMIN-002
```

هذه الحالة يجب إعادة التحقق منها قبل أي جلسة تنفيذ؛ لا تعتبر الـSHA أعلاه دليلًا على Green لاحقًا.
