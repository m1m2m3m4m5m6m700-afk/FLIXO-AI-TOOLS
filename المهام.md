# P01 — FLIXO MASTER DEVELOPMENT MAP
- STATUS: ACTIVE / CANONICAL TASK GATE
- SOURCE OF TRUTH: `PROJECTS.md` → `المهام.md` → `AGENTS.md`

## المرحلة A — التشغيل المستمر والوكالات
- Canonical execution remains on `execution`; repair and verification are exact-SHA bound.
- Resident automation uses evidence-driven wake/recovery and never substitutes stale evidence for current state.

## ترتيب التنفيذ الإلزامي
1. DISCOVER → READ_INBOX → REVALIDATE_EXACT_SHA
2. RCA → FALSIFY → PRE-MUTATION PROOF
3. IMPLEMENT → TARGETED REGRESSION → AFFECTED CONTRACT VERIFY
4. EXACT_SHA_CHECK → CANONICAL CI → CERTIFICATION

## بوابات الخروج
- GREEN requires fresh exact-SHA canonical evidence.
- Any actionable RED remains open until its causal source is repaired and reverified.
- `execution → main` is the only publication path; no force-push.

# P0 — FULL-SYSTEM RED TEAM — 2026-09-24

### RTA-SYSTEM-001 — Required workflow source spoofing
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- FINDING: Required workflow evidence was selected by display name + SHA without canonical workflow-path binding.
- REPAIR: Watcher, Daily Green Gate, and Auto Repair Merge Gate now bind each required workflow to its canonical `.github/workflows/*.yml` path.

### RTA-SYSTEM-002 — Latest cancellation fallback
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- FINDING: Merge/Daily settlement filtered out cancelled runs before choosing the latest run, allowing older success evidence to survive a newer cancellation.
- REPAIR: Latest canonical run is selected before conclusion evaluation; an unsuperseded cancellation remains RED.

### RTA-SYSTEM-003 — Repair intake workspace gate
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- FINDING: `hashFiles('/tmp/run-id')` was used to gate steps using a path outside `GITHUB_WORKSPACE`.
- REPAIR: Downstream intake now gates on an explicit resolver step output and binds run name to canonical workflow path.

### RTA-SYSTEM-004 — Privileged wake source revalidation
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- FINDING: Master Activation and Watchdog consumed workflow_run event identity before independently revalidating source path and live execution SHA.
- REPAIR: Canonical name/path, branch, conclusion, and live execution SHA are checked before privileged dispatch/wake.

### RTA-SYSTEM-005 — Assistant wake transport mismatch
- STATUS: IN_REPAIR / FRESH-LIVE-RUNTIME-VERIFICATION-REQUIRED
- FINDING: Live Council wake triggers used `net.http_get` with credential material in query parameters, while `assistant-channel` rejects credentialed GET requests.
- REPAIR: New migration `20260924024500_master3_assistant_wake_post_transport.sql` switches trigger and retry transport to authenticated POST bodies and preserves `service_role`-only execution.

### RTA-SYSTEM-006 — Proof validator parser failure
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- FINDING: scripts/ci/validate-runtime-proof-coverage.mjs failed at parse time on the canonical HEAD proof read expression.
- REPAIR: replace nested template-literal path construction with explicit string concatenation; no proof rule was removed.

### RTA-SYSTEM-007 — Security Red-Team contract drift
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- FINDING: the Red-Team contract expected a textual git ls-files -z form while the runner correctly uses execFileSync('git', ['ls-files', '-z']).
- REPAIR: test now asserts the actual safe argv-array invocation.

### RTA-SYSTEM-008 — Shared-memory audience contract drift
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- FINDING: validator treated role aliases as direct global audience members even though the canonical audience is the 200 FLIXO-BOT-* identities and aliases resolve through botAliasMap.
- REPAIR: validator now checks exact targetCount=200 and authoritative alias resolution instead of weakening the 200-bot invariant.

### RTA-SYSTEM-009 — Certification surface workflow classification drift
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- FINDING: canonical security-red-team and engineering work-package workflows were misclassified as non-canonical automated workflows by the certification-surface validator.
- REPAIR: security-critical registry membership and explicit engineering-guard classification now count as admitted automation; unknown push/pull_request automation remains fail-closed.

### Closure rule
- Code mutation is not closure.
- Closure requires fresh exact-SHA canonical CI, adversarial negative controls, zero false-green paths, and no unresolved actionable RED.
# P0 — RED TEAM TEST-SYSTEM CERTIFICATION SOURCE-SPOOFING — 2026-09-24

### RTA-CERT-002
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- FINDING: Certification evidence was selected by check name + exact SHA without proving that the check belonged to the canonical `FLIXO Test System` run.
- ATTACK: an unrelated `Certification` check can carry `success` and the current SHA while the canonical Test System run is different or absent.
- RCA: the reducer consumed a name-matched check independently from the canonical workflow-run identity.
- REPAIR: Certification now requires `success`, exact current SHA, an Actions run identity, and exact linkage to the selected canonical `FLIXO Test System` run.
- NEGATIVE CONTROLS: current-SHA Certification from another run and current-SHA Certification without a run link must remain `RED_INTERNAL`.
- SAFETY: no test weakening, no authority change, no dispatch change, no historical evidence reuse.
- CLOSURE: OPEN until fresh exact-SHA CI proves source-spoofing cannot reduce the test system to GREEN.

# P00 — COUNCIL LIVENESS / GUARDIAN P0 WORK PACKAGE — 2026-09-24

### WP-COUNCIL-LIVENESS-P0-20260924
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- BASELINE_SHA: `3ce11b36b805e5544cd1abe43465310f0da5112e`
- SCOPE: External Guardian v3, bounded lease recovery, false-health prevention, resident heartbeat exact-SHA proof, logical-vs-live runtime semantics, assistant query credential rejection, Supersession parser hardening.
- RECOVERY STATES: `RECOVERED` / `BLOCKED` / `FAILED_TERMINAL`.
- RESIDENCY: 100 logical profiles are distinct from 10 live runtime seats; live proof requires heartbeat evidence and current execution SHA.
- EVIDENCE RULE: started CI evidence remains immutable; queued/pending stale work is supersedable.
- CURRENT RCA: Supersession settlement had a stray `)` after a jq command; repaired and guarded against regression.
- CURRENT POLICY RCA: Work Package Guard interpreted one-work-package-per-burst as per-commit tagging; policy now requires exactly one explicit WP identifier per repair burst and rejects conflicting/missing WP identifiers.
- CLOSURE: OPEN until canonical CI, live runtime verification, and deployment verification are fresh on the final exact SHA.
# P00 — FIVE-BOT ACTIVE COHORT / 100-BOT ROTATION — 2026-09-24

### FIVE-BOT-ROTATION-001
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- POLICY: 5 Active logical bots only; 20 cohorts × 5 complete one 100-bot cycle, then wrap to CELL-001.
- COMMITMENT: 15 minutes per active cohort. This is an operational commitment boundary, not task completion.
- RUNTIME: 10 verified runtime seats remain available; exactly 5 are admitted ACTIVE and 5 are STAGED/READY.
- HANDOFF: current cohort may not be released merely because 15 minutes elapsed; the exact next five must publish READY against the same exact execution SHA.
- NO-GAP RULE: missing next-cohort readiness causes HANDOFF_PENDING + recovery/reclaim, never SLEEP/IDLE and never silent exit.
- TASK-LIFETIME: an open task has no fixed terminal deadline. It survives worker/session timeout and continues through the next cohort until canonical GREEN/COMPLETE.
- CYCLE: CELL-001..CELL-005 → CELL-006..CELL-010 → ... → CELL-096..CELL-100 → CELL-001..CELL-005.
- SOURCE OF TRUTH: `scripts/ci/agent-liveness-protocol.mjs` + `scripts/ci/five-bot-rotation.mjs` + `المهام.md`.
- REQUIRED PROOF: fresh exact-SHA canonical CI, five-active admission evidence, next-five READY handoff evidence, 20-cohort wrap evidence, and no false GREEN.
- LATEST RCA: Heartbeat now pins all cohort/liveness execution to a detached exact `execution` SHA worktree; the trusted `main` checkout is no longer used for execution-only controller modules.
- LATEST RCA: Supersession final stale-run reporting had an unmatched `)`; the syntax is corrected and regression-guarded.
- HANDOFF: five staged workers publish `readySignal=true` with `readyExactSha`; the active cohort cannot advance until the next five pass the exact-SHA handoff gate.
- CLOSURE: OPEN until runtime evidence proves the handoff chain without active-count overflow or early release.

# P00 — CI EVIDENCE-PRESERVATION CONTRACT RCA — 2026-09-24

### REPAIR-BOT-WAKE-CI-001
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- RCA-ID: `RCA-WAKE-CI-CONTRACT-001`
- SESSION_ID: `EXEC-MASTER-AGENT2-20260924-001`
- BASELINE_SHA: `6738710d3bc658d0298a3892b502c08ae74656c4`
- REPAIR_SHA: `3d78e4f0b03aeedbf094a5b1bb7a257d8318eb27`
- OPERATION_COUNT: 25
- OPERATION_DIGEST: `a810b2f7d02999d4f629ac98dc8aa5922c4a24a5512a0fb4b16954c1f61f1c4c`
- OBSERVED_FAILURE: `FLIXO Test System` failed `Static + Build → validate:ci-contract` on the exact baseline SHA because `wp0-trust-baseline.yml` had `cancel-in-progress: false` while the validator treated it as a cancelling exact-SHA workflow.
- RCA: the validator's `nonCancellingExactShaEvidence` set covered `ci.yml` and `test-impact.yml` but omitted WP0, Test Impact Execution, and Repository Security Baseline, despite those workflows preserving started exact-SHA evidence and validating supersession explicitly.
- REPAIR: extend `nonCancellingExactShaEvidence` to the three omitted verification workflows. No workflow authority, mutation lane, retry semantics, or runtime behavior changed.
- TARGETED_TEST: `node scripts/validate-ci-contract.mjs`
- REQUIRED_PROOF: fresh Static + Build, WP0, Test Impact, Security, Auto Repair Merge Gate, then full canonical CI on the resulting exact SHA.
- CLOSURE: OPEN until exact-SHA canonical GREEN and runtime verification are both fresh.
- LEARNING: cancellation policy belongs to the evidence class; verifier must preserve started exact-SHA proof while supersession controllers handle stale queued/pending work.
- NEXT: continue on the resulting execution SHA; invalidate prior evidence.

# P00 — OFFICIAL TEST-SYSTEM TAKEOVER / NO-RESURRECTION CONTRACT — 2026-09-24

### TEST-SYSTEM-TAKEOVER-001
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- BASELINE_SHA: `6738710d3bc658d0298a3892b502c08ae74656c4`
- RCA: Latest-Commit Supersession was cancelling stale verification work but resident Heartbeat had an independent `*/5` schedule and was globally protected from supersession. Each scheduled trigger could create another resident window, while current-SHA runs were intentionally retained. Consequently cleanup removed symptoms but the scheduler recreated the workload.
- OFFICIAL RULE: only the trusted `execution-bot-watchdog.yml` owns the periodic resident Heartbeat bootstrap. Heartbeat keeps its 60-second internal cadence and may be manually workflow-dispatched for controlled recovery, but it has no independent periodic schedule.
- STALE-RUN RULE: queued/pending stale Heartbeat runs are supersedable; started Heartbeat runs remain immutable evidence and are not cancelled merely because a newer SHA exists.
- EXACT-SHA RULE: one active resident bootstrap is admitted per current execution SHA; Watchdog checks active current-SHA Heartbeat before dispatch and NOOPs when one exists.
- TEST-SYSTEM OWNERSHIP: `.github/workflows/ci.yml` remains the canonical test engine; supersession controls stale run population and never closes task authority.
- CLOSURE: OPEN until fresh exact-SHA CI proves no recurring Heartbeat queue multiplication and sustained resident continuity is evidenced.

# P00 — HEARTBEAT READ-ONLY / WATCHDOG GUARDIAN TEST ALIGNMENT — 2026-09-24

### REPAIR-BOT-WAKE-012
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- BASELINE_SHA: `a73f26979c11948839d2dab7752f61aa3a88c3a9`
- RCA: Advanced Repair Contract failed because `validate-auto-repair-boundary.mjs` required an explicit guardian-lane declaration that the current Heartbeat workflow did not state, while the supervision regression still required the retired direct Watchdog dispatch from Heartbeat. Both assertions conflicted with the current read-only Heartbeat → privileged Watchdog architecture.
- REPAIR: make the canonical Heartbeat explicitly declare the single trusted Watchdog guardian lane and align the supervision regression to require the declaration while rejecting direct Watchdog API dispatch from Heartbeat. No Actions-write authority or new dispatch authority is added.
- REQUIRED PROOF: fresh exact-SHA Advanced Repair Contract, Test System/WP0/Impact/Security evidence, then sustained resident Heartbeat and Watchdog automatic-renewal proof.
- CLOSURE: OPEN until canonical GREEN + sustained residency.

# P00 — HEARTBEAT BOUNDARY CONTRACT REPAIR — 2026-09-24

### REPAIR-BOT-WAKE-011
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- BASELINE_SHA: `e5b8912ebeff6ee0129abe4f239ac48742a13a37`
- RCA: Advanced Repair Contract still encoded the retired Heartbeat `actions: write` guardian contract after the Heartbeat was intentionally reduced to read-only evidence emission.
- REPAIR: validator now requires `actions: read` + no `actions: write`, while trusted Watchdog retains the privileged resident-window renewal authority.
- REQUIRED PROOF: Advanced Repair Contract GREEN, then WP0/Test System/Impact/Heartbeat evidence on the same exact SHA; sustained residency remains open until consecutive windows are proven.
- CLOSURE: OPEN until canonical GREEN + sustained residency.

# P00 — RESIDENT WAKE + EVIDENCE-STABILITY REPAIR — 2026-09-24

### REPAIR-BOT-WAKE-010
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- BASELINE_SHA: `a23d2f42cd5127f73d2ec95438136fef04fee60d`
- RCA-1: Repository Security Baseline, WP0, and Test Impact Execution used `cancel-in-progress: true`; push/PR overlap could cancel started evidence.
- RCA-2: WP0/Test System static lint had two concrete source errors: undefined `sha` and unused `masterActivation`.
- RCA-3: Heartbeat held Actions-write authority and attempted guardian dispatch itself, conflicting with the read-only wake-plane boundary.
- REPAIR: preserve started evidence; move resident-window renewal to trusted Watchdog; keep Heartbeat read-only with one differentiated FLIXO1..FLIXO10 pulse each minute for a 45-minute active window.
- COUNCIL RELAY: a push without explicit `PRESIDENT WAKE` remains intentionally skipped; this is a policy guard, not a bot-sleep failure.
- REQUIRED PROOF: fresh exact-SHA WP0/Test System/Impact/Advanced Repair Contract/Repository Security evidence, multiple Heartbeat minute ticks with 10-bot ACTION-WAKE, and Watchdog proof of automatic renewal.
- CLOSURE: OPEN until canonical GREEN plus sustained residency proof.

# P00 — WATCHDOG VALIDATOR CONTRACT ALIGNMENT — 2026-09-24

### REPAIR-BOT-WAKE-009
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- BASELINE_SHA: `10a0f0b8af49cf0833d5b8000126ffd4df019f5a`
- RCA: the runtime Watchdog correctly moved from repeated GitHub Installation API ref reads to `git ls-remote` to avoid rate-limit exhaustion, but `scripts/validate-ci-contract.mjs` still required the obsolete `gh api` implementation string.
- REPAIR: validator source marker is now aligned with the canonical rate-limit-safe exact-SHA observer. The validator continues to require stale-event rejection, exact 40-character SHA, trusted-main checkout, and immutable run concurrency.
- REQUIRED PROOF: fresh Test System/WP0/Impact evidence on the new exact SHA plus a real resident Heartbeat window with one-minute pulses.
- CLOSURE: OPEN until canonical GREEN and sustained residency proof.

# P00 — 45-MINUTE RESIDENT WINDOW ALIGNMENT — 2026-09-24

### REPAIR-BOT-WAKE-007
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- BASELINE_SHA: `09d4a3b36fa8511610d35c6e2a3a5837a9589514`
- RCA: `AGENT_LIVENESS_PROTOCOL` requires a 45-minute minimum active window, while Heartbeat executed only 10 one-minute ticks (600s). That allowed a resident run to terminate before the protocol's closure threshold.
- REPAIR: Heartbeat now executes 45 one-minute pulses, uses a 50-minute job timeout, and emits a 2700-second window proof. The supervision contract is aligned to the same 45-tick requirement.
- REQUIRED PROOF: one resident Heartbeat completes the full 45-minute active window, maintains exact live execution SHA rollover handling, emits one ACTION-WAKE pulse per minute, keeps sleep/idle false, and then the next resident wake becomes available automatically.
- CLOSURE: OPEN until sustained residency and canonical exact-SHA GREEN are both proven.

# P00 — SUPERSESSION QUEUE ROOT-CAUSE HARDENING — 2026-09-24

### REPAIR-BOT-WAKE-006
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- BASELINE_SHA: `09e482a879e0cff3038c09c694e401b6e088ffd9`
- RCA: the stale-run controller treated every Heartbeat as resident-protected, including queued/pending windows. This allowed old heartbeat windows to accumulate behind scheduler pressure instead of cancelling stale queued windows while preserving the single started resident window.
- REPAIR: classify Heartbeat as supersedable for queued/pending stale runs; retain the existing immutable-evidence rule that started/in-progress runs are never cancelled.
- REQUIRED PROOF: queued stale Heartbeats are removed, one started Heartbeat remains resident, minute pulses continue across execution-head rollover, and no current-SHA evidence is cancelled as stale.
- CLOSURE: OPEN until canonical exact-SHA GREEN plus sustained residency evidence.

# P00 — HEARTBEAT HEAD-ROLLOVER + LINT ROOT-CAUSE REPAIR — 2026-09-24

### REPAIR-BOT-WAKE-005
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- BASELINE_SHA: `6e8eaeafaf07e58e6097d2387a466a1e1aee4a07`
- RCA: resident Heartbeat was fail-closing after the first minute whenever another repair agent advanced `execution`; the resident controller compared the live execution SHA to its original event SHA. This made normal repair progress terminate the resident window and prevented consecutive wake pulses.
- ADDITIONAL CI ROOT CAUSE: WP0 static lint had two concrete source errors: undefined `sha` in `test-agent-liveness-protocol.mjs` and an unused `masterActivation` binding in `test-repair-supervision-contract.mjs`.
- REPAIR: keep exact-SHA targeting but re-resolve the live `execution` SHA at every minute; record a controlled head rollover instead of terminating the resident wake window. Repair and test-system contracts are corrected without weakening fail-closed behavior.
- REQUIRED PROOF: two or more consecutive minute ticks, ACTION-WAKE evidence for all FLIXO1..FLIXO10, no sleep/idle, no false GREEN, and fresh WP0/Test System/Impact/Advanced Repair Contract evidence on the resulting SHA.
- CLOSURE: remains OPEN until canonical exact-SHA GREEN and sustained residency evidence are both present.

# P00 — WAKE RATE-LIMIT ROOT-CAUSE REPAIR — 2026-09-24

### REPAIR-BOT-WAKE-003
- STATUS: IN_REPAIR / CANONICAL-CI-VERIFICATION-PENDING
- TARGET_SHA: `0bbd6846c8eecb904d4679d0603d098cd512ce19`
- OBSERVED_FAILURE: after fixing `GH_TOKEN`, the first heartbeat still failed with GitHub Installation API HTTP 403 rate limiting at `git/ref/heads/execution`.
- RCA: the one-minute resident loop performed a GitHub Installation API source-head lookup on every tick; the observer/watchdog path also repeated the same installation-API ref lookup. This amplified quota consumption before the actual wake packet could run.
- REPAIR: replace source-head reads with public `git ls-remote` resolution for `refs/heads/execution`, while retaining GitHub API authentication only for privileged Actions/run operations that actually require it.
- OBSERVER REPAIR: use the same public ref resolution when `repair-session-observer.mjs` needs the live execution SHA.
- SAFETY: exact-SHA comparison remains mandatory; stale events remain fail-closed; no third branch, no force push, no test skipping, no false GREEN, and the Daily Green Gate remains the sole repair dispatcher.
- REMAINING: fresh exact-SHA CI must show the Heartbeat reaches the FLIXO-10 wake packet without Installation API rate-limit failure, then prove consecutive one-minute ticks and watchdog recovery.

# P00 — REPAIR-BOT WAKE PATH ROOT-CAUSE REPAIR — 2026-09-24

### REPAIR-BOT-WAKE-002
- STATUS: IN_REPAIR / CANONICAL-CI-VERIFICATION-PENDING
- TARGET_SHA: `531d8d093b44f7f5f4d9883a7d137fd2b221e3a6`
- OBSERVED_FAILURE: `FLIXO Agent Repair Heartbeat` failed at the first minute with `gh: To use GitHub CLI in a GitHub Actions workflow, set the GH_TOKEN environment variable.`
- RCA: the heartbeat `Emit canonical one-minute repair heartbeat` step invoked `gh api` without `GH_TOKEN`; the workflow already had `actions: write`, but authentication was not exported to the failing step.
- REPAIR: bind `GH_TOKEN=${{ github.token }}` directly to the heartbeat emission step; preserve exact-SHA reads, FLIXO-10 wake generation, `RESIDENT_SLEEP=false`, `RESIDENT_IDLE=false`, and the single canonical Watchdog guardian lane.
- TEST REPAIR: replace stale supervision assertions for retired bootstrap/300-tick behavior with assertions for the live 10-worker per-minute wake contract and the authenticated CLI path.
- SAFETY: no test skipping, no false GREEN, no direct `main` mutation, no force push, no third branch, no Auto Repair direct dispatch from Heartbeat.
- REMAINING: fresh exact-SHA CI must prove the first heartbeat reaches FLIXO-10 wake, subsequent minute ticks continue, Watchdog recovery remains live, and no actionable task enters passive sleep/idle.

# P00 — 100-BOT GITHUB RESIDENCY + REAL WAKE PATH — 2026-09-23

### TEST-SYSTEM-RESIDENCY-100-001
- STATUS: `OPEN / SUSTAINABILITY-PROOF-PENDING`
- CLOSURE POLICY: لا يوجد ضمان مسبق للاستدامة؛ التشغيل يستمر تلقائيًا حتى يثبت فشل/توقف فعلي. التنفيذ أو Run واحد ناجح لا يغلق المهمة.
- RESIDENCY: 300 one-minute pulses per window (5h), leaving a 45-minute runtime overhead buffer, within the GitHub-hosted 6-hour job limit; the Resident executes from `execution`, is ignited directly by `push: execution` and trusted scheduled bootstrap, and re-dispatches itself on `execution` through `workflow_dispatch`.
- REAL_WAKE: every pulse emits `ACTION-WAKE` for all 10 FLIXO1..FLIXO10 Action Repair identities; active work or actionable current-SHA RED re-enters Daily·FLIXO Green Gate; only clean idle state scans read-only.
- NONSTOP: the re-arm step runs with `if: always()`, including failed/non-green windows.
- SUSTAINABILITY PROOF: require multiple consecutive resident windows on `execution`, no unexplained interval >60 seconds, exact-SHA continuity, successful next-window dispatch, and fresh Action-Wake evidence from the next window.
- IMPORTANT: CELL-001..CELL-100 remain logical development residents only; the historical CELL worker pool remains retired.
- SAFETY: no test skip, no false green, no main mutation, no force-push, no third branch, no new authority, no parallel mutation workers.
- CURRENT: الـHeartbeat هو الـresident الوحيد: يبدأ عبر bootstrap من Test System، يعمل 300 نبضة × دقيقة، ويعيد تشغيل نفسه عبر `workflow_dispatch` على `execution`; مسار Watchdog أصبح مراقبًا يدويًا فقط.
- REMAINING: إثبات Run فعلي مستمر للـHeartbeat وظهور نبضات `ACTION-WAKE` المتتابعة؛ ثم التحقق من إعادة التشغيل التلقائي بعد نهاية كل نافذة. عند ثبوت فشل أو توقف يعود المسار للإصلاح، ولا تُغلق المهمة قبل وجود دليل استدامة فعلي.
- CURRENT EXTERNAL STATE: GitHub Actions يعرض حاليًا 61 Run في `queued/pending` و0 `in_progress`؛ لا تُعتبر هذه حالة نوم للبوتات ولا GREEN، بل `BLOCKED_EXTERNAL` على مستوى scheduler حتى يبدأ Run فعلي.

# P00 — TEST SYSTEM TYPECHECK FOLLOW-UP — 2026-09-23

# P00 — TEST SYSTEM CHAIR-ADMISSION FULL-INTELLIGENCE CLOSURE — 2026-09-23

- STATUS: IN_REPAIR
# P00 — WAKE / SUPSERSESSION / RESIDENCY HARDENING — 2026-09-24

### REPAIR-BOT-WAKE-004
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-VERIFICATION-REQUIRED
- SCOPE: Heartbeat guardian → Watchdog single lane, started-run preservation, supersession jq hardening, empty-ledger residency fallback.
- SAFETY: Heartbeat executes trusted controller code from main; direct Auto Repair / Green Gate dispatch remains forbidden from Heartbeat.
- VERIFICATION: required on the resulting execution SHA; no GREEN is inferred from code changes alone.
- LATEST RCA: canonical certification rejected Heartbeat and latest-execution cleanup as non-canonical automated workflows; Task Agent lacked the mandatory GREEN-boundary marker.
- THIS CYCLE: trust-classified those automated workflows and restored the missing Task Agent marker; source changes do not imply GREEN.
- LATEST INTERNAL RCA: WP0/impact validation requires event-scoped branch concurrency, and the agent-protocol validator was still asserting retired legacy task-ledger headings that are absent from the current canonical ledger.
- REPAIR: bind WP0 concurrency to repository+PR/ref identity and make agent-protocol task-gateway assertions target the current P00/P01 canonical task structure. GREEN remains evidence-only.


## 3) P0 — ERROR INTELLIGENCE
- Boundary: exact-SHA RCA → evidence → repair → verification → learning.
- Closure: official CI evidence only; Task Agent does not declare GREEN.

# P0 — RED TEAM TEST-SYSTEM CERTIFICATION FALSE-GREEN REPAIR — 2026-09-24

### RTA-CERT-001
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-CI-REQUIRED
- FINDING: \`continuous-error-watch.mjs\` could mark the monitored state \`GREEN\` while the latest \`Certification\` check was \`failure/neutral/cancelled\`, or while a successful Certification check was bound to an old or missing SHA.
- RCA: the Green reducer recorded Certification state but did not make its success or exact-head identity a mandatory GREEN invariant.
- REPAIR: Certification must be present, \`success\`, and carry an exact 40-character head SHA equal to the monitored execution SHA. Failure/neutral/cancelled/missing-SHA/stale-SHA states now create explicit RED errors and cannot reduce to GREEN.
- NEGATIVE CONTROLS: added tests for \`failure\`, \`neutral\`, \`cancelled\`, stale \`head_sha\`, and missing certification SHA, plus a current-SHA positive control.
- SAFETY: no test weakening, no mutation authority change, no branch-topology change, no historical evidence reuse.
- REQUIRED PROOF: targeted \`node scripts/ci/test-continuous-error-watch.mjs\`, fresh Test System/WP0/Impact/Security evidence, canonical Certification, then full exact-SHA CI.
- CLOSURE: OPEN until the resulting execution SHA proves all negative controls fail closed and canonical GREEN remains impossible for invalid Certification evidence.


## مزامنة دليل الإصدار — 2026-09-24

الرأس التنفيذي المثبت لهذه الدورة هو 41d44f132fa27892155fa0ab47fb1452b908b380. C4 مكتمل بنجاح، بينما Work Package Guard أحمر، و npm run verify غير مثبت مباشرة على نفس الرأس. main الحالي في لقطة المراجعة هو 1a26c4a3624ec7ae58a8c52191246dda423de5bb. لا يُنقل أي دليل أقدم إلى رأس أحدث.
