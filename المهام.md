# FLIXO-AI-TOOLS — سجل التنفيذ الموحد

> TASK-LEDGER v3.0 — GREEN-FIRST / EXECUTABLE
>
> هذا الملف هو سجل التنفيذ الوحيد. الأولوية الحالية: إثبات GREEN فعليًا على آخر SHA. بعد GREEN فقط تُفتح مراحل المنصة.

## 0) MASTER EXECUTION PROMPT

```text
أنت الوكيل التنفيذي الرئيسي لمستودع FLIXO-AI-TOOLS.

المهمة العليا: الوصول إلى GREEN حقيقي مثبت بالأدلة، ثم فقط متابعة خارطة المنصة.

القواعد:
1. READ قبل CHANGE.
2. EXACT SHA هو مصدر الحقيقة.
3. لا تعتمد على دليل تاريخي كدليل حالي.
4. لا تغلق المهمة بسبب نجاح جزئي.
5. QUEUED/CANCELLED/SKIPPED ليست PASS.
6. لا تعيد تشغيل الفشل بلا RCA.
7. لكل RED: FINGERPRINT → RCA → REPRODUCE → REPAIR → TARGETED REGRESSION → FULL CI.
8. بعد كل commit: أعد بناء evidence من الصفر للـSHA الجديد.
9. السبب الخارجي = BLOCKED_EXTERNAL، وليس GREEN.
10. لا تضعف اختبارًا أو Security Gate لإخفاء فشل.
11. لا توسع النطاق أثناء إصلاح RED إلا عند إثبات الضرورة.
12. استخدم أقل تغيير يزيل السبب الجذري.
13. افحص Error Memory قبل فرضية جديدة.
14. عند عدم كفاية الدليل: UNKNOWN_RCA، لا تخمين.
15. لا تبدأ Platform/Product task قبل GREEN.
16. لا يعلن الوكيل GREEN؛ الأدلة الرسمية هي المرجع.
17. لا تغلق الجلسة طالما يوجد Required RED أو RCA مفتوح.
18. بعد GREEN راقب أول دورة post-green قبل فتح مرحلة جديدة.
```

## 1) P0 — GREEN-RECOVERY-001

STATUS = IN_PROGRESS / BLOCKING

المسار الإجباري:
READ → INVENTORY → FINGERPRINT → RCA → REPRODUCE → REPAIR → TARGETED TEST → FULL CI → EXACT-SHA RECHECK → REPEAT

قائمة الإغلاق:
- [ ] تحديد آخر execution SHA.
- [ ] جرد جميع Required Checks.
- [ ] لا Required run في QUEUED/IN_PROGRESS.
- [ ] لا Required CANCELLED/SKIPPED.
- [ ] Test System PASS.
- [ ] Test Impact PASS.
- [ ] Test Impact Execution PASS.
- [ ] WP0 Trust Baseline PASS.
- [ ] Static/Typecheck/Build PASS.
- [ ] Browser required suites PASS.
- [ ] Certification PASS.
- [ ] Security/Code Scanning المطلوبة PASS.
- [ ] Targeted regressions PASS.
- [ ] لا OPEN_RCA.
- [ ] Exact-SHA evidence مكتمل.
- [ ] لا تغييرات غير معلنة.
- [ ] PROJECTS.md و المهام.md متزامنان.
- [ ] GREEN مثبت على آخر SHA.

### PROMPT — تشخيص وإصلاح RED

```text
خذ أول Required RED حقيقي فقط.
اقرأ run + job + logs + workflow + script/contract المرتبط.
استخرج: failureFingerprint, entrySha, runId, trigger, propagationPath, violatedInvariant, causalSource, classification, confidence, falsificationCheck.
ابحث في Error Memory عن fingerprint مشابه، ثم أعد إثباته على SHA الحالي.
لا تعدّل قبل RCA قابل للاختبار.
نفّذ reproduction أو أقوى تحقق متاح.
طبّق أقل إصلاح جذري.
شغّل targeted regression ثم Full Required CI.
إذا فشل الإصلاح: لا تكرر نفس الفرضية؛ أعد RCA.
إذا ظهر failure جديد: افتح RCA جديدًا وحافظ على evidence القديم.
```

## 2) P0 — AGENT EXECUTION CONTROL

AGENT-WORK-001..008

LOGIN → TASK CLAIM → SCOPE LOCK → DEPENDENCY CHECK → PROGRESS/FINDING/CHANGE/TEST → HANDOFF → EXIT VERIFICATION

قواعد: لا عمل بلا Task ID. لا تعديل خارج scope بلا تسجيل. لا agent self-authorization. كل تغيير مرتبط بـSHA ودليل. conflict/overlap = BLOCK حتى يُحسم.

## 3) P0 — ERROR INTELLIGENCE

ERROR-AGENT: CI Evidence → Fingerprint → Memory → RCA → Task Packet → Repair → Regression

RCA المقبول = trigger → propagation → violated invariant → causal source → symptom.
حالات عدم اليقين: UNKNOWN_RCA | BLOCKED | EXTERNAL.
ممنوع التخمين أو masking أو retry-until-green أو test weakening أو إعلان VERIFIED/GREEN.

## 4) P1 — FOUNDATION AFTER GREEN

كل ما يلي BLOCKED-UNTIL-GREEN:
- PLATFORM-ARCH-001 — توحيد Foundation فوق Registry/Execution/Verification الحالية.
- CAPABILITY-BUILDER-001 — Gap Detection + Candidate Contract.
- CAPABILITY-BUILDER-002 — Candidate Implementation + Sandbox.
- FLIXO-PLATFORM-001 — Visual Intelligence Platform.
- FLIXO-PLATFORM-002 — Capability Evolution Loop.
- FLIXO-PLATFORM-003 — Image Intelligence Roadmap.

## 5) P1 — IMAGE CAPABILITIES

بعد Foundation: SELECT → REMOVE → REPLACE → INPAINT → OUTPAINT.
ثم Image Control → Enhancement → Composition → Export → Product/Commerce → Smart Workflows.
كل capability = CONTRACT → REGISTRY → EXECUTION → VERIFICATION → FINAL QA → PERFORMANCE → SECURITY/PRIVACY → ERROR MEMORY.

## 6) P1 — QUALITY / PERFORMANCE / PRODUCT

- FILTERS-001 — Filters/Presets داخل Catalog موحد.
- PERFORMANCE-ENGINE-001 — محرك صورة واحد + Pipeline واحد.
- PERFORMANCE-COMPETITIVE-001 — Benchmark reproducible مع quality/p50/p95/memory/responsiveness.
- FINAL-QA-001 — Understand → Plan → Execute → Inspect → Repair → Verify → Deliver.
- RESPONSIVE-UI-001 — واجهة Mobile/Desktop واحدة مع حفظ Editing Session.
- AGENT-FALLBACK-001 — Agent failure ≠ App outage؛ fallback إلى Manual Image Catalog + Error Memory.

## 7) P2 — DEFERRED

- FLIXO-PLATFORM-004 — Developer/Extension Surface.
- FLIXO-PLATFORM-005 — Enterprise Readiness.
لا يبدأ أي منهما قبل Foundation + GREEN.

## 8) DEPENDENCY GRAPH

GREEN-RECOVERY-001 → AGENT CONTROL → ERROR INTELLIGENCE → PLATFORM-ARCH-001 → CAPABILITY-BUILDER-002 → PLATFORM-001 → PLATFORM-002 → PLATFORM-003 → SELECT → REMOVE → REPLACE → INPAINT → OUTPAINT → FINAL-QA/PERFORMANCE/SECURITY → RESPONSIVE → DEVELOPER/ENTERPRISE

قاعدة: لا تتجاوز عقدة فاشلة إلى العقدة التالية.

## 9) DONE / GREEN CONTRACT

```text
GREEN = EXACT CURRENT SHA ∧ REQUIRED CHECKS PASS ∧ SECURITY PASS ∧ STATIC/TYPECHECK/BUILD PASS ∧ BROWSER PASS ∧ CERTIFICATION PASS ∧ TARGETED REGRESSION PASS ∧ NO OPEN RCA ∧ FRESH EVIDENCE ∧ DECLARED SCOPE
```
أي عنصر ناقص = NOT GREEN.

## 10) ANTI-COMPLEXITY

SEARCH EXISTING → REUSE → EXTEND → MERGE → CREATE ONLY IF NECESSARY.
ممنوع Registry ثانية، Execution Engine ثانية، QA Engine ثانية، Error Memory ثانية، duplicate catalogs، mass generation بلا Gap Evidence، autonomous production promotion، autonomous Security/Verifier mutation.

## 11) EXECUTION MEMORY

لكل إصلاح: Task ID + Fingerprint + Entry SHA + Run ID + RCA + Falsification Check + Changed Files + Repair + Targeted Test + Full CI + Final SHA + Evidence + Prevention Lesson.
الذاكرة معرفة مساعدة وليست دليل نجاح؛ الدليل الحالي يجب أن يأتي من SHA الحالي.

## 12) FINAL OPERATING PROMPT

```text
لا تبحث عن GREEN؛ ابنِ الدليل الذي يثبت GREEN.
ابدأ من آخر SHA. اقرأ كل Required failure. استخرج RCA. اختبر الفرضية. أصلح الجذر. اختبر الإصلاح. شغّل CI الكامل. راقب SHA الجديد. كرر حتى تزول كل RED المطلوبة.
لا تخفِ الفشل. لا تضعف الحواجز. لا تتجاوز RCA. لا تنتقل إلى Platform قبل GREEN.
```

## 13) EXCEPTIONAL IDEAS REVIEW

| الفكرة | الحالة |
|---|---|
| GREEN-first gate | ACTIVE |
| Exact-SHA evidence | ACTIVE |
| RCA + fingerprint + Error Memory | ACTIVE |
| Independent Verification + Final QA | ACTIVE |
| Capability Gap → Candidate → Sandbox | ACTIVE |
| Automatic test generation | CANDIDATE |
| Predictive capability discovery | CANDIDATE |
| Developer Extensions | DEFERRED |
| Enterprise Layer | DEFERRED |
| Autonomous production promotion | CANCELLED |
| Security/Verifier self-modification | CANCELLED |
| Duplicate Registry/Execution/QA | CANCELLED |
| Skipped/Cancelled = PASS | CANCELLED |
| Retry loops بلا RCA | CANCELLED |