# P00 — WAKE RATE-LIMIT ROOT-CAUSE REPAIR — 2026-09-24

### REPAIR-BOT-WAKE-003
- STATUS: IN_REPAIR / CANONICAL-CI-VERIFICATION-PENDING
- TARGET_SHA: `0bbd6846c8eecb904d4679d0603d098cd512ce19`
- OBSERVED_FAILURE: after fixing `GH_TOKEN`, the first heartbeat still failed with GitHub Installation API HTTP 403 rate limiting at `git/ref/heads/execution`.
- RCA: the one-minute resident loop performed a GitHub Installation API source-head lookup on every tick; the observer/watchdog path also repeated the same installation-API ref lookup. This amplified quota consumption before the actual wake packet could run.
- REPAIR: replace source-head reads with public `git ls-remote` resolution for `refs/heads/execution`, while retaining GitHub API authentication only for privileged Actions/run operations that actually require it.
- OBSERVER REPAIR: use the same public ref resolution when `repair-session-observer.mjs` needs the live execution SHA.
- SAFETY: exact-SHA comparison remains mandatory; stale events remain fail-closed; no third branch, no force push, no test skipping, no false GREEN, and the Daily Green Gate remains the sole repair dispatcher.
- REMAINING: fresh exact-SHA CI must show the Heartbeat reaches the FLIXO-10 wake packet without Installation API rate-limit failure, then prove consecutive one-minute ticks and watchdog recovery.

# P00 — REPAIR-BOT WAKE PATH ROOT-CAUSE REPAIR — 2026-09-24

### REPAIR-BOT-WAKE-002
- STATUS: IN_REPAIR / CANONICAL-CI-VERIFICATION-PENDING
- TARGET_SHA: `531d8d093b44f7f5f4d9883a7d137fd2b221e3a6`
- OBSERVED_FAILURE: `FLIXO Agent Repair Heartbeat` failed at the first minute with `gh: To use GitHub CLI in a GitHub Actions workflow, set the GH_TOKEN environment variable.`
- RCA: the heartbeat `Emit canonical one-minute repair heartbeat` step invoked `gh api` without `GH_TOKEN`; the workflow already had `actions: write`, but authentication was not exported to the failing step.
- REPAIR: bind `GH_TOKEN=${{ github.token }}` directly to the heartbeat emission step; preserve exact-SHA reads, FLIXO-10 wake generation, `RESIDENT_SLEEP=false`, `RESIDENT_IDLE=false`, and the single canonical Watchdog guardian lane.
- TEST REPAIR: replace stale supervision assertions for retired bootstrap/300-tick behavior with assertions for the live 10-worker per-minute wake contract and the authenticated CLI path.
- SAFETY: no test skipping, no false GREEN, no direct `main` mutation, no force push, no third branch, no Auto Repair direct dispatch from Heartbeat.
- REMAINING: fresh exact-SHA CI must prove the first heartbeat reaches FLIXO-10 wake, subsequent minute ticks continue, Watchdog recovery remains live, and no actionable task enters passive sleep/idle.

# P00 — 100-BOT GITHUB RESIDENCY + REAL WAKE PATH — 2026-09-23

### TEST-SYSTEM-RESIDENCY-100-001
- STATUS: `OPEN / SUSTAINABILITY-PROOF-PENDING`
- CLOSURE POLICY: لا يوجد ضمان مسبق للاستدامة؛ التشغيل يستمر تلقائيًا حتى يثبت فشل/توقف فعلي. التنفيذ أو Run واحد ناجح لا يغلق المهمة.
- RESIDENCY: 300 one-minute pulses per window (5h), leaving a 45-minute runtime overhead buffer, within the GitHub-hosted 6-hour job limit; the Resident executes from `execution`, is ignited directly by `push: execution` and trusted scheduled bootstrap, and re-dispatches itself on `execution` through `workflow_dispatch`.
- REAL_WAKE: every pulse emits `ACTION-WAKE` for all 10 FLIXO1..FLIXO10 Action Repair identities; active work or actionable current-SHA RED re-enters Daily·FLIXO Green Gate; only clean idle state scans read-only.
- NONSTOP: the re-arm step runs with `if: always()`, including failed/non-green windows.
- SUSTAINABILITY PROOF: require multiple consecutive resident windows on `execution`, no unexplained interval >60 seconds, exact-SHA continuity, successful next-window dispatch, and fresh Action-Wake evidence from the next window.
- IMPORTANT: CELL-001..CELL-100 remain logical development residents only; the historical CELL worker pool remains retired.
- SAFETY: no test skip, no false green, no main mutation, no force-push, no third branch, no new authority, no parallel mutation workers.
- CURRENT: الـHeartbeat هو الـresident الوحيد: يبدأ عبر bootstrap من Test System، يعمل 300 نبضة × دقيقة، ويعيد تشغيل نفسه عبر `workflow_dispatch` على `execution`; مسار Watchdog أصبح مراقبًا يدويًا فقط.
- REMAINING: إثبات Run فعلي مستمر للـHeartbeat وظهور نبضات `ACTION-WAKE` المتتابعة؛ ثم التحقق من إعادة التشغيل التلقائي بعد نهاية كل نافذة. عند ثبوت فشل أو توقف يعود المسار للإصلاح، ولا تُغلق المهمة قبل وجود دليل استدامة فعلي.
- CURRENT EXTERNAL STATE: GitHub Actions يعرض حاليًا 61 Run في `queued/pending` و0 `in_progress`؛ لا تُعتبر هذه حالة نوم للبوتات ولا GREEN، بل `BLOCKED_EXTERNAL` على مستوى scheduler حتى يبدأ Run فعلي.

# P00 — TEST SYSTEM TYPECHECK FOLLOW-UP — 2026-09-23

# P00 — TEST SYSTEM CHAIR-ADMISSION FULL-INTELLIGENCE CLOSURE — 2026-09-23

- STATUS: IN_REPAIR
# P00 — WAKE / SUPSERSESSION / RESIDENCY HARDENING — 2026-09-24

### REPAIR-BOT-WAKE-004
- STATUS: IN_REPAIR / FRESH-EXACT-SHA-VERIFICATION-REQUIRED
- SCOPE: Heartbeat guardian → Watchdog single lane, started-run preservation, supersession jq hardening, empty-ledger residency fallback.
- SAFETY: Heartbeat executes trusted controller code from main; direct Auto Repair / Green Gate dispatch remains forbidden from Heartbeat.
- VERIFICATION: required on the resulting execution SHA; no GREEN is inferred from code changes alone.
