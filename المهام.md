# P00 — 100-BOT GITHUB RESIDENCY + REAL WAKE PATH — 2026-09-23

### TEST-SYSTEM-RESIDENCY-100-001
- STATUS: `IMPLEMENTED / EXACT-SHA-CI-VERIFICATION-PENDING`
- PULSE: one real heartbeat every 60 seconds; pulse profile rotates by the UTC minute across the 10 existing FLIXO1..FLIXO10 roles; every pulse carries `WAKE_ALL_AGENTS` and the complete resident development profiles.
- REAL_WAKE: every pulse emits `ACTION-WAKE` for all 10 Action Repair identities. If there is active repair/test work **or an actionable completed RED on the current execution SHA**, the heartbeat re-enters the existing canonical `Daily·FLIXO Green Gate` repair path instead of treating the repository as idle.
- IDLE: only when there is no active operation and no actionable RED does the pulse perform the full repository read-only sweep and regenerate the development plan.
- CONTINUITY: six one-minute wake pulses per residency window; the five-minute scheduled wake remains the fallback. No `workflow_run → main` rearm is required for the minute pulse path.
- IMPORTANT: `CELL-001..CELL-100` remain logical development residents only; the historical CELL worker pool remains retired. Actual Action Repair execution remains the existing FLIXO1..FLIXO10 cohort with one active mutation worker.
- SAFETY: no test skip, no false green, no main mutation, no force-push, no third branch, no new authority, no parallel mutation workers.
- REMAINING: fresh exact-SHA CI must prove the heartbeat Run starts on `execution`, each minute emits an `ACTION-WAKE`, and any current-SHA RED causes Green Gate re-entry.

# P00 — TEST SYSTEM TYPECHECK FOLLOW-UP — 2026-09-23

# P00 — TEST SYSTEM CHAIR-ADMISSION FULL-INTELLIGENCE CLOSURE — 2026-09-23

- STATUS: IN_REPAIR