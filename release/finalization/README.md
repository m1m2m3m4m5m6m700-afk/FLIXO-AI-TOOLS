# S5 Finalization

This directory contains the release-finalization contracts only.

The manifests intentionally remain non-green until CI produces complete evidence tied to one exact release SHA. A cancelled, missing, or mixed-SHA evidence set cannot be promoted to release certification.
