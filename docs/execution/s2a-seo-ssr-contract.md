# S2A — SSR SEO Integration

This change set integrates the existing SEO contract into the public `/` and `/ar/` route heads without changing the application-wide locale default.

Required contract: canonical URL plus the full 20-locale hreflang set and `x-default`, OpenGraph, and Twitter metadata.
