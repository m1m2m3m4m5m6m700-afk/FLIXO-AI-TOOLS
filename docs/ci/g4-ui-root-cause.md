# G4 UI localization Root Cause

The post-#511 G4 run exposed a final shared set of English-first UI strings. They are covered centrally in `tool-ui-runtime-supplement.ts` without changing G4 assertions or adding an allowlist.
