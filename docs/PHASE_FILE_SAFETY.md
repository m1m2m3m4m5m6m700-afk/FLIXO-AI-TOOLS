# Shared Browser Image File Safety

The shared BrowserImageTool validates file name, MIME type, byte size, and decoded dimensions/pixel budget before processing. SVG Optimizer uses an explicit SVG MIME policy; raster tools use the bounded raster image policy.
