# CI/CD trigger marker

This file records the CI/CD promotion verification repair on `main`.

The delivery pipeline promotes only an exact main SHA that has a successful `FLIXO Test System` run and a matching immutable build artifact.
