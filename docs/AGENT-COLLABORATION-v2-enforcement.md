# Enforcement

The local hook layer provides immediate fail-closed protection. The CI layer repeats the checks from a clean exact-SHA checkout. Cross-branch validation inspects open same-repository agent PRs and rejects stale claims and scope collisions. The cooperation graph remains the contract dependency authority. The coordination dashboard and evidence artifacts are observability only.