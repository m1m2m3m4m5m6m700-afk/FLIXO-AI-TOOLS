# Evidence Requirements

Every agent event must bind identity, action, branch and exact SHA. Verification packets should include the commands run, changed files, contract IDs, Root Cause IDs, artifact paths, blockers and next action. Release claims require fresh exact-HEAD evidence from the canonical certification authority.