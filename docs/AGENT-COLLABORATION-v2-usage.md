# Usage

For a writer, check in with an exact SHA and disjoint scope before work. Renew with heartbeat. On SHA change, stop writes and re-ingest. Use a handoff packet for transfer. Check out when complete. CI validates the registry, session projection, cross-branch collisions and cooperation graph. The dashboard/evidence files are observability surfaces, never ownership authority.