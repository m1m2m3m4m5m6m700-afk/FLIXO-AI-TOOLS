# CI Speed V4 Final

V4 keeps required verification independent and fail-closed while removing redundant canonical reruns.

Unknown change scope must resolve to full verification; no skipped evidence is treated as success.
