# Integration blocker

The repository currently has an open PR #474 with an in-progress S4 runtime gate. The V5–V10 branch is intentionally based on its head SHA. The final workflow replacement must be merged only after #474 reaches a stable green state, otherwise the certification graph would be measuring a moving target.
