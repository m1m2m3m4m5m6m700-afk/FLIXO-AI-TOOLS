#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';const r=JSON.parse(fs.readFileSync(path.resolve(process.cwd(),'docs/agents/CELL-BOT-REGISTRY.json'),'utf8'));const out=process.env.FLIXO_CELL_WAKE_OUTPUT??'/tmp/flixo-cell-wake.json';const sha=String(process.env.FLIXO_WAKE_SHA??'').trim();
if(!/^[a-f0-9]{40}$/u.test(sha))throw new Error('CELL_WAKE_EXACT_SHA_REQUIRED');if(r.status!=='RETIRED'||r.bots?.length!==0)throw new Error('CELL_WAKE_RETIREMENT_STATE_INVALID');
const packet={schemaVersion:2,authority:'CELL_WAKE_PREPARATION',exactSha:sha,readiness:'RETIRED',wholeCell:{botCount:0},directMutation:false,directRepairDispatch:false,reason:'CELL_POOL_RETIRED_BY_USER_DIRECTIVE',generatedAt:new Date().toISOString()};fs.mkdirSync(path.dirname(out),{recursive:true});fs.writeFileSync(out,JSON.stringify(packet,null,2)+'\n');console.log(JSON.stringify(packet,null,2));