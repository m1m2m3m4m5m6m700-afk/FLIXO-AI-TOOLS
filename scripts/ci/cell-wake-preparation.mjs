#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';const r=JSON.parse(fs.readFileSync(path.resolve(process.cwd(),'docs/agents/CELL-BOT-REGISTRY.json'),'utf8'));const out=process.env.FLIXO_CELL_WAKE_OUTPUT??'/tmp/flixo-cell-wake.json';const sha=String(process.env.FLIXO_WAKE_SHA??'').trim();
if(!/^[a-f0-9]{40}$/u.test(sha))throw new Error('CELL_WAKE_EXACT_SHA_REQUIRED');if(r.status!=='RETIRED'||r.bots?.length!==0||r.logicalRoster?.count!==100||r.logicalRoster?.runtimeSeatCount!==10)throw new Error('CELL_WAKE_LOGICAL_RUNTIME_CONTRACT_INVALID');
const packet={schemaVersion:3,authority:'CELL_WAKE_PREPARATION',exactSha:sha,readiness:'AWAKE_AND_READY',wholeCell:{botCount:100,logicalBotCount:100,runtimeSeatCount:10,runtimeSeatIds:['FLIXO1','FLIXO2','FLIXO3','FLIXO4','FLIXO5','FLIXO6','FLIXO7','FLIXO8','FLIXO9','FLIXO10']},directMutation:false,directRepairDispatch:false,logicalOnly:true,reason:'LOGICAL_100_ON_TEN_VERIFIED_RUNTIME_SEATS',generatedAt:new Date().toISOString()};fs.mkdirSync(path.dirname(out),{recursive:true});fs.writeFileSync(out,JSON.stringify(packet,null,2)+'\n');console.log(JSON.stringify(packet,null,2));