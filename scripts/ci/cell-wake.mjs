#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';const r=JSON.parse(fs.readFileSync(path.resolve(process.cwd(),'docs/agents/CELL-BOT-REGISTRY.json'),'utf8'));if(r.status!=='RETIRED'||r.bots?.length!==0)throw new Error('CELL_WAKE_RETIREMENT_STATE_INVALID');console.log(JSON.stringify({status:'RETIRED',botCount:0,reason:'CELL_POOL_RETIRED_BY_USER_DIRECTIVE'},null,2));