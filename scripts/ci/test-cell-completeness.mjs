import assert from 'node:assert/strict';import fs from 'node:fs';const r=JSON.parse(fs.readFileSync('docs/agents/CELL-BOT-REGISTRY.json','utf8'));const i=JSON.parse(fs.readFileSync('diagnostics/auto-repair/cell-knowledge/index.json','utf8'));assert.equal(r.status,'RETIRED');assert.equal(r.bots.length,0);assert.equal(i.status,'RETIRED');assert.equal(i.totalBots,0);assert.equal(i.bots.length,0);assert.equal(i.deletedPoolFiles,400);console.log('CELL_200_POOL_REMOVED=PASS');