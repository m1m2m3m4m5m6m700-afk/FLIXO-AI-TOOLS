#!/usr/bin/env node
import assert from 'node:assert/strict';import {spawnSync} from 'node:child_process';import {execFileSync} from 'node:child_process';const sha=execFileSync('git',['rev-parse','HEAD'],{encoding:'utf8'}).trim();const p=spawnSync(process.execPath,['scripts/ci/cell-wake-preparation.mjs'],{encoding:'utf8',env:{...process.env,FLIXO_WAKE_SHA:sha,FLIXO_CELL_WAKE_OUTPUT:'/tmp/flixo-cell-retired.json'}});assert.equal(p.status,0);assert.match(p.stdout,/RETIRED/u);const w=spawnSync(process.execPath,['scripts/ci/cell-wake.mjs'],{encoding:'utf8'});assert.equal(w.status,0);assert.match(w.stdout,/RETIRED/u);console.log('CELL_WAKE_RETIREMENT=PASS');