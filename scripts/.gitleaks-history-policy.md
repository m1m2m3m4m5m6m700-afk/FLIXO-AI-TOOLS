# Gitleaks history policy

The repository's `.gitleaks.toml` intentionally scopes two historical false-positive rule matches by exact commit and exact path. It does not suppress current or future findings elsewhere.
