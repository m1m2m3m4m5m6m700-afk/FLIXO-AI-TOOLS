name: FLIXO CI — G2 File Safety Shadow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    name: G2 File Safety Shadow — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "G2 File Safety Shadow is disabled; Executive Contract CI-016 owns input/file-safety validation."
