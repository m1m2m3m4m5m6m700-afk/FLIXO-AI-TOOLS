name: FLIXO CI — Toolchain Build Shadow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    name: Toolchain Build Shadow — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Toolchain Build Shadow is disabled; Executive Contract owns build validation."
