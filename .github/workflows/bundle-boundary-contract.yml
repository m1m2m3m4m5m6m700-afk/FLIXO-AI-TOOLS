name: Bundle Boundary Contract

# Legacy gate retained for manual investigation only.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Bundle Boundary Contract — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Bundle Boundary Contract is disabled; Executive Contract CI-011 owns bundle/performance validation."
