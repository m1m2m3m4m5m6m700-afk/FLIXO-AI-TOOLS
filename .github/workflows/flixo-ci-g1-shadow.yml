name: FLIXO CI — G1 Contract Shadow

# Shadow evaluator is retained for manual forensic comparison only.
# Canonical G1 certification owns automated PR/main execution.
on:
  workflow_dispatch:

concurrency:
  group: flixo-ci-g1-manual
  cancel-in-progress: true

permissions:
  contents: read

env:
  VITE_SITE_URL: https://flixoai.vercel.app
  VITE_RUNTIME_ORIGIN: http://127.0.0.1:3000
  VITE_TEST_ORIGIN: http://127.0.0.1:3000

jobs:
  g1-shadow:
    name: G1 Contract Shadow (manual diagnostic)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci --prefer-offline --no-audit --no-fund
      - name: Validate CI contract registry
        run: node --experimental-strip-types scripts/ci/contracts/validate-registry.mjs
      - name: TypeScript
        run: npx tsc --noEmit --pretty false
      - name: Legacy G1 evaluator in diagnostic shadow mode
        run: node --import=./scripts/register-node-resolver.mjs --experimental-strip-types scripts/test-platform-contract.mjs
      - name: Upload G1 shadow evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: ci-g1-shadow-${{ github.sha }}
          path: |
            artifacts/ci/
            dist/
          if-no-files-found: warn
          retention-days: 14
