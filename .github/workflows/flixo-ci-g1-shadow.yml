name: FLIXO CI — G1 Contract Shadow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    name: G1 Contract Shadow — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "G1 Contract Shadow is disabled; Executive Contract CI-003/CI-015 own the relevant validation."
