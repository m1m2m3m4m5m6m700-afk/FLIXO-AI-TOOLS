name: CI — Legacy (Disabled)

# The Executive Contract is now the single PR verification authority.
# This legacy workflow remains available for manual emergency runs only.
# Final GREEN/RED decisions are owned by .github/workflows/ci-executive.yml.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Legacy CI disabled — Executive Contract owns verification
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: echo "Legacy CI is disabled. Use the Executive Contract (CI-001..CI-018) and its Aggregator for the authoritative result."
