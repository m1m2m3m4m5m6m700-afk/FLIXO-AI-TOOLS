name: Full Matrix Parallel (retired)

# Canonical DEEP browser execution now lives in .github/workflows/ci.yml as browser-deep.
# Retained only as a manual forensic stub; no duplicate browser execution is allowed.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Retired — use canonical Browser DEEP engine
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Full Matrix Parallel is retired. Canonical DEEP browser coverage is browser-deep in FLIXO Test System.'
