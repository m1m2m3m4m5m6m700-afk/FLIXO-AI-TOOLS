name: Full Matrix Parallel

# Legacy diagnostic workflow retained for manual investigation only.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Full Matrix Parallel — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Full Matrix Parallel is disabled; use CI-006/CI-007/CI-008 in the Executive Contract."
