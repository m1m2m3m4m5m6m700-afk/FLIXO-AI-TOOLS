name: FLIXO CI — Contract Registry Shadow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    name: Contract Registry Shadow — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Contract Registry Shadow is disabled; Executive Contract CI-003 owns registry and contract validation."
