name: FLIXO CI — Execution Engine Shadow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    name: Execution Engine Shadow — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Execution Engine Shadow is disabled; Executive Contract owns execution-path verification."
