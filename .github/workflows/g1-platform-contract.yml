name: G1 Platform Contract Gate

# Legacy gate retained for manual investigation only.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: G1 Platform Contract — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "G1 Platform Contract Gate is disabled; Executive Contract CI-003 owns registry and contract validation."
