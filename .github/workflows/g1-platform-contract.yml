name: G1 Platform Contract Gate

# G1 is a deep certification gate. PR verification is owned by the canonical
# impact-aware CI path; this workflow certifies the resulting main state.
on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: g1-platform-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  g1-platform-contract:
    name: G1 Platform Contract
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      VITE_SITE_URL: https://flixoai.vercel.app
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci --prefer-offline --no-audit --no-fund
      - name: Run consolidated G1 contract
        run: node --import=./scripts/register-node-resolver.mjs --experimental-strip-types scripts/test-platform-contract.mjs
