name: Matrix First Gate (retired)

# Canonical FAST browser execution now lives in .github/workflows/ci.yml as browser-fast.
# This workflow is retained only as a manual forensic stub; it must never duplicate execution.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Retired — use canonical Browser FAST engine
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Matrix First is retired. Canonical FAST browser coverage is browser-fast in FLIXO Test System.'
