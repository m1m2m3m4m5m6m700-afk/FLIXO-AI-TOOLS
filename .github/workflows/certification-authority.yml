name: Global Certification Authority (retired)

# Certification is now the single `certify` job in .github/workflows/ci.yml.
# This stub is retained only for manual forensic history and never certifies a release.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Retired — use canonical Certification gate
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Global Certification Authority is retired. Canonical certification is the certify job in FLIXO Test System.'
