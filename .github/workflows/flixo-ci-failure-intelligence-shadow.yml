name: FLIXO CI — Failure Intelligence Shadow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    name: Failure Intelligence Shadow — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Failure Intelligence Shadow is disabled; Executive Contract owns failure extraction and aggregation."
