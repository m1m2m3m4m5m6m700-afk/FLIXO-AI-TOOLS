name: FLIXO CI — Foundation Shadow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    name: Foundation Shadow — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Foundation Shadow is disabled; Executive Contract CI-001/CI-003 own the required checks."
