name: FLIXO CI — Impact Analysis Shadow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    name: Impact Analysis Shadow — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Impact Analysis Shadow is disabled; Executive Contract remains authoritative."
