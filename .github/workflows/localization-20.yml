name: G4 — Localization + SEO Matrix

# Deep G4 certification is part of the canonical PR/main CI path.
# This workflow is retained only as a manual compatibility entry point and does not duplicate G4 execution.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    name: G4 moved to canonical CI
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical execution
        run: echo 'Deep G4 runs in .github/workflows/ci.yml as the required fail-fast merge gate.'
