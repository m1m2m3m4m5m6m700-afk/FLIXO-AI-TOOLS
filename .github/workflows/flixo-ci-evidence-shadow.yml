name: FLIXO CI — Evidence Ledger Shadow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    name: Evidence Ledger Shadow — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Evidence Ledger Shadow is disabled; Executive Contract owns per-job evidence and error-report.json."
