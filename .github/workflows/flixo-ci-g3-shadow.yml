name: FLIXO CI — G3 Artifact Integrity Shadow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    name: G3 Artifact Integrity Shadow — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "G3 Artifact Integrity Shadow is disabled; Executive Contract owns bundle/artifact validation."
