name: Parallel Diagnostics

# Deep-diagnostic workflow. It intentionally does not replace the canonical CI gate.
# 23 suites × 3 browsers = 69 independent suite/browser diagnostic executions.

on:
  workflow_dispatch:
  pull_request:
    paths:
      - '.github/workflows/parallel-diagnostics.yml'

permissions:
  contents: read

env:
  NPM_CONFIG_AUDIT: 'false'
  NPM_CONFIG_FUND: 'false'
  NPM_CONFIG_UPDATE_NOTIFIER: 'false'

concurrency:
  group: parallel-diagnostics-${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  e2e:
    name: Diagnostic — ${{ matrix.tool }} — ${{ matrix.browser }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      max-parallel: 64
      matrix:
        browser: [chromium, firefox, webkit]
        tool:
          - image-compressor
          - background-remover
          - image-upscaler
          - image-converter
          - ai-image-generator
          - object-remover
          - watermark-remover
          - image-cropper
          - image-to-svg
          - image-ocr
          - photo-colorizer
          - background-blur
          - passport-photo-maker
          - watermark-adder
          - meme-generator
          - collage-maker
          - image-effects
          - exif-cleaner
          - svg-optimizer
          - mockup-generator
          - seed
          - pix
          - localization
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm
      - name: Install dependencies
        run: npm ci --prefer-offline --no-audit --no-fund
      - name: Cache Playwright ${{ matrix.browser }}
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ matrix.browser }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-${{ matrix.browser }}-
      - name: Sanitize blocked external APT sources
        shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r source; do
            echo "Removing inaccessible Microsoft APT source: $source"
            sudo rm -f "$source"
          done < <(grep -RIl 'packages\.microsoft\.com' /etc/apt/sources.list.d 2>/dev/null || true)
      - name: Install Playwright system dependencies
        run: npx playwright install-deps ${{ matrix.browser }}
      - name: Install Playwright browser
        run: npx playwright install ${{ matrix.browser }}
      - name: Resolve test specification
        id: spec
        shell: bash
        run: |
          set -euo pipefail
          spec="tests/${{ matrix.tool }}.spec.ts"
          if [[ "${{ matrix.tool }}" == "localization" ]]; then
            spec="tests/localization-smoke.spec.ts"
          fi
          test -f "$spec"
          echo "spec=$spec" >> "$GITHUB_OUTPUT"
      - name: Run diagnostic E2E
        env:
          CI: 'true'
        run: npx playwright test "${{ steps.spec.outputs.spec }}" --project=${{ matrix.browser }}
      - name: Upload diagnostic evidence
        if: ${{ always() && failure() }}
        uses: actions/upload-artifact@v6
        with:
          name: diagnostic-${{ matrix.tool }}-${{ matrix.browser }}
          path: |
            playwright-report/
            test-results/
          if-no-files-found: warn
          retention-days: 14

  summary:
    name: Diagnostic Summary
    if: ${{ always() }}
    needs: [e2e]
    runs-on: ubuntu-latest
    steps:
      - name: Write summary
        run: |
          {
            echo '## FLIXO Parallel Diagnostic Matrix'
            echo
            echo 'Matrix design: 23 suites × 3 browsers = 69 independent diagnostic jobs.'
            echo 'max-parallel=64 uses the highest practical matrix parallelism while GitHub applies the account concurrency ceiling.'
            echo 'fail-fast=false keeps independent failures visible.'
            echo 'Any failed matrix job leaves the diagnostic workflow red.'
            echo 'Overall matrix result: ${{ needs.e2e.result }}'
          } >> "$GITHUB_STEP_SUMMARY"
