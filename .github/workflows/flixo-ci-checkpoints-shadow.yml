name: FLIXO CI — Checkpoint Shadow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    name: Checkpoint Shadow — disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Checkpoint Shadow is disabled; Executive Contract is authoritative."
