# تنفيذ الذكاء الاصطناعي

## الحالة

الهدف من هذا الملف هو سجل التنفيذ الموحد لمسار ترقية الذكاء الاصطناعي في FLIXO-AI-TOOLS.
القاعدة الحاكمة: Agent ليس Authority؛ الـCanonical Registry هو Authority.

## المرحلة الأولى — منفذة

تم دمج الحزمة الأولى في مسار execution:

1. Failure Intelligence Agent
   - يبني fingerprint حتمي للفشل.
   - يطبع failure features وnormalized failure.
   - يربط الدليل مباشرة بمسار الاستهداف بدل إنشاء فرضيات غير قابلة للإثبات.

2. Exact Targeting Agent
   - يعيد استخدام الاختبار/الملف الموجود.
   - يمنع إنشاء اختبارات جديدة.
   - يتحقق من هوية هدف Playwright عبر --list ويغلق التنفيذ عند zero/multiple matches.
   - يبقى baseline=3 وafter=2 داخل عقد التحقق الموجود في Auto-Repair.

3. Impact Engine
   - يستخدم محرك scripts/ci/impact/engine.ts الموجود بدل إنشاء محرك أثر منافس.
   - يحسب العقود المتأثرة ومستوى التصعيد L0-L3 من الملفات المتغيرة.
   - أي ملف غير مغطى بقاعدة يصبح conservative L3.

4. CI Orchestrator
   - لا ينفذ workflows بنفسه.
   - يبني خطة حتمية: targeted-only أو targeted+static/build أو full canonical CI.
   - جهة الإرسال الوحيدة تبقى Daily·FLIXO Green Gate.
   - جهة الإصلاح الوحيدة تبقى FLIXO Auto Repair Bot.

5. Concurrency Guard
   - يثبت expected SHA == local SHA == origin/execution SHA قبل وبعد مرحلة الإصلاح.
   - أي حركة متزامنة للرأس تفتح fail-closed بدل دمج أو overwrite.

## المرحلة الثانية — منفذة

Phase 1 → Phase 2 → Phase 3 أصبحت سلسلة واحدة عبر FLIXO-CANONICAL-REPAIR-CONTEXT وcontextHash. Phase 3 تستهلك العقدة نفسها ولا تعيد اكتشاف مصدر الحقيقة.

### Repair Strategy Agent
توحيد اختيار الاستراتيجية مع `repair-strategy.mjs` عبر دوران حتمي مرتبط بالمحاولة، مع إبقاء repair attempt إلزاميًا.

### Test Set-Cover Minimizer
تطبيق Weighted Greedy Set Cover على `scripts/ci/test-impact-map.json` لاختيار أقل مجموعة أوامر تغطي مجالات التغيير؛ الملفات المجهولة تتحول إلى full canonical verification.

### Security Guardian Agent
حارس fail-closed يمنع الأسرار وworkflow mutations وcontrol-plane mutations ويعيد استخدام `repository-security-baseline.mjs` كمرجع.

### Release Integrity Agent
تثبيت سلسلة source SHA → execution SHA → PR head → evidence → main/promotion → merge → deployment identity.

### Canonical Repair Context Bridge — Phase 1 → Phase 2
Phase 1 أصبحت منتج السياق canonical وليست مجرد تقرير تشخيص. كل POSTFLIGHT يولد FLIXO-CANONICAL-REPAIR-CONTEXT مع SHA المصدر، fingerprint الفشل، الهدف، الملفات المتغيرة، الأثر، وخطة CI، ويحمل contextHash حتميًا.

Phase 2 تستهلك السياق نفسه بدل إعادة اكتشاف المدخلات من مسارات مستقلة. تتحقق من contextHash، تثبت أن sourceSha يطابق HEAD أثناء دورة الإصلاح، تقارن changedFiles مع working tree، ثم تعيد حساب Impact بشكل مستقل لمنع drift قبل Set-Cover أو Security Guard.

Release Handoff لا يمر إلا عندما يبقى contextHash نفسه من Phase 1 إلى Phase 2، وتطابق evidence الـsource SHA وfailure fingerprint، ثم تُستكمل سلسلة parent/execution/PR/evidence.

## المرحلة الثالثة — منفذة

### Failure Clustering
تم التنفيذ كـ connected-component clustering حتمي باستخدام fingerprint وroot-cause وتشابه tokens/features، بدون mutation.

### Bayesian Confidence Calibration
تم التنفيذ باستخدام Beta-Binomial مع prior صريح، مع استبعاد blocked-external من denominator، وإخراج posterior confidence وevidence weight.

### Contextual Bandit Strategy Selection
تم التنفيذ كـ bounded deterministic UCB فوق Bayesian posterior وخصائص السياق، مع top-3 exploration وبدون randomness، مع بقاء النتيجة advisory-only.

### Predictive Anomaly Detection
تم التنفيذ كإشارة recurrence/cluster pressure bounded: SIGNAL_ONLY، ولا تمنع الإصلاح ولا تعدّل المستودع ولا تعلن GREEN.

### Learning / Evolution Agent
تم التنفيذ كـ promotion-candidate generator للقواعد التي تملك ≥2 fingerprints ناجحة و≥2 repairs و≥80% success rate، مع exact-SHA وCanonical CI وrollback قبل التفعيل.

## مبادئ التنفيذ

- مصدر الحقيقة واحد لكل قدرة.
- لا Agent يعدل المستودع مباشرة خارج executor الموثوق.
- لا Agent يعلن GREEN.
- لا اختبار جديد ينشأ تلقائيًا من فشل لم يثبت exact target.
- Targeted verification ليست بديلًا عن Canonical CI.
- أي ambiguity أو concurrency drift = fail closed.
- لا third branch.
- لا self-dispatch.
- لا source mutation لإخفاقات provider/external tooling.

## Exceptional Ideas Review

ACTIVE
- Failure Intelligence
- Exact Targeting
- Impact Graph/Engine
- CI Orchestration Plan
- Concurrency Guard
- Canonical Repair Context Bridge
- Security Guardian
- Release Integrity
- Test Set-Cover Minimizer

CANDIDATE
- none

NEEDS DEVELOPMENT
- none

DEFER
- General free-execution autonomous agent
- Agent قادر على إنشاء/تعديل الاختبارات دون registry evidence

CANCELLED
- Agents متنافسة تعدل المستودع مباشرة
- Dispatchers متعددة لنفس الإصلاح

## أمر التنفيذ للمراحل التالية

المرحلة الثانية تُنفذ بعد تثبيت المرحلة الأولى على exact SHA وإعادة إثبات canonical CI.
المرحلة الثالثة مبنية فوق نتائج المرحلتين الأولى والثانية. الخوارزميات الاحتمالية بقيت محصورة في طبقة advisory ولا تدخل سلطة mutation أو GREEN؛ تفعيل أي evolution candidate يتطلب calibration وrollback وevidence.


## V12 — CAUSAL REPAIR INTELLIGENCE — منفذة

تم رفع وكيل اختيار الإصلاح من دوران استراتيجيات ثابت إلى محفظة استدلال سببي موحدة داخل `scripts/ci/repair-strategy.mjs`.

المكونات:
- Causal Reasoning: ربط السبب الجذري مع fingerprint والسجل والسياق والكود.
- Hypothesis Portfolio: الفرضية الرئيسية + أقوى بديل + مسار تشخيص مستقل + تصعيد.
- Falsification Plan: لا mutation قبل محاولة تفنيد الفرضية البديلة وإثبات trigger → propagation → source → symptom.
- Strategy Learning: الاستراتيجية تُختار من root-cause fit + history + contextual success + rejection ledger + twin signal.
- Anti-Loop: الاستراتيجية المرفوضة/المعاد تكرارها لا تعاد دون evidence delta.
- Security Surface Scan: كشف إشارات input-taint وGITHUB_ENV وartifact provenance وworkflow permissions وcomment transport وtrusted-workflow boundaries؛ النتائج إشارات تحقيق وليست سلطة GREEN.
- Authority Separation: V12 لا تنفذ mutation بذاتها ولا تعلن GREEN؛ executor وCanonical CI يحتفظان بالسلطة.

## جذور تم إصلاحها أثناء ترقية V12

- `RCA-001`: drift بين تنفيذ `execution-bot-watchdog.yml` وعقد التحقق الثابت في `validate-ci-contract.mjs`. تم توحيد العقد مع الصيغة الفعلية للـexact-SHA checkout.
- `RCA-002`: فشل سابق في نقل بعض رسائل الإصلاح عبر `gh issue comment` موثق كـGraphQL integration restriction؛ بقي كهدف نقل/قناة وليس كإصلاح source وهمي.
- `RCA-003`: `advanced-repair-contract.yml` كان موجودًا ويحقق PASS لكنه غير مسجل في canonical control-plane registry، مما جعل certification-surface يصنفه non-canonical. تم تسجيله في `REPAIR_GATE_AUTOMATION` بدل إضافة استثناء محلي.

## EXACT-SHA CHECKPOINT

آخر SHA ترقية موثقة:
`fa9e2dffdd6d9f7a0cd1099400fa802a903d2efc`

قاعدة السجل:
أي SHA جديد يلغي أدلة الإصلاح السابقة، ويجب إعادة targeted regression + affected-contract verification + canonical CI قبل الإغلاق.

## ERROR-ONLY SEMANTIC PROGRAMMER — UPGRADED
STATUS = IMPLEMENTED / BOUNDED

The existing Error-Only Programmer has been upgraded without introducing a second Registry, Memory, Gate, Agent, or repair engine.

Implemented:
- Central protocol enforcement for exact affected-source sets.
- Test and control-plane mutation blocking at protocol level.
- Semantic source slice evidence: file, failing line, bounded context, nearest declaration/symbol when available.
- Deterministic driver metadata and stable strategyKey for anti-loop learning.
- Deterministic TS2304 missing-import repair only when one tracked exported symbol is uniquely proven.
- Isolated simulation validates the declared source-file set.
- Agent-1 prompt and protocol validator are bound to the same contract.

Unsupported failure classes remain proposal-only by design.
