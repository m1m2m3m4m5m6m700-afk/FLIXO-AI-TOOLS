import type { LocalizedToolSeo } from '../manifest';
export const en: LocalizedToolSeo = { title:'Image to Text OCR Online', description:'Extract text from images directly in your browser with OCR.', intro:'Upload an image, run local OCR preprocessing, and download the detected text as a TXT file.', keywords:['image to text','OCR online','extract text from image'], howTo:['Upload an image.','Run OCR.','Review and download the text.'], features:['Browser-based OCR','Dedicated Web Worker','TXT download'], altText:['Image to text OCR tool','OCR result preview','Extracted text download'] };
