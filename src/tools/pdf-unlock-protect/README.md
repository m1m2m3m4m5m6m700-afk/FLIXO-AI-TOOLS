# PDF Unlock & Protect

Client-side PDF protection and unlocking for FLIXO. Files remain in browser memory during processing.

The tool uses pinned browser-loaded security engines and exposes protect/unlock output contracts in Playwright coverage.
